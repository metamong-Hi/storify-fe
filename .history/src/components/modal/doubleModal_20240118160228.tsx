import React, { useState } from 'react';
import OneModal from './singleModal';

const doubleModal = () => {
  const [firstModalShow, setFirstModalShow] = useState(false);
  const [secondModalShow, setSecondModalShow] = useState(false);

  return (
    <div>

    </div>
  );
};

export default doubleModal;
